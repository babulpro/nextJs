import React from 'react';

const template = ({children}) => {
    return (
        <div className='w-full min-h-svh'>
          
                      <div >{children}</div>
            
        </div>

    );
};

export default template;